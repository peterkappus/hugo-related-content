languageCode: en-us
title: My New Hugo Site
related:
  includeNewer: true #include references to posts newer than the current one
  indices:
    - name: keywords 
      weight: 1 # We don't really need this, but omitting it would disable the indice.
